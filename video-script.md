# Kyma 宣传视频脚本

> 总时长：约 1 分 15 秒 | 四段结构：开头 → 重大特性 → 新语法 → 结尾

---

## 一、开头（0:00 – 0:15）

| 时间 | 画面 | 旁白 |
|------|------|------|
| 0:00 – 0:04 | 纯白屏幕。一个深色光标在中央闪烁，伴随键盘敲击声。逐字打出 `@title("Kyma")`，字体为等宽体，打字动画。 | （静默，仅键盘音效） |
| 0:04 – 0:08 | 代码行继续向下生长：`@key(D, major)`、`@tempo(108)`、`@time(4/4)`。背景从纯白渐变为柔和的浅蓝-淡紫渐变，代码以深色等宽字体呈现。 | "如果写一首曲子，可以像写一段代码一样——" |
| 0:08 – 0:12 | 画面拉远，代码悬浮在空间中央，镜头缓慢推进。底部浮现一行小字：`toki-musi · the music language`。 | "用人类可读的语法，描述音符、和弦、表情——" |
| 0:12 – 0:15 | Logo 动画：**Kyma** 字样从波形中浮现，希腊字母 κ 的笔画化为声波。副标题：*A Rust Music Composition Toolkit*。 | "这就是 Kyma。" |

---

## 二、重大特性（0:15 – 0:40）

| 时间 | 画面 | 旁白 |
|------|------|------|
| 0:15 – 0:20 | 四宫格卡片依次弹入，每张卡片代表一个 crate：**sonus**（纯音乐理论模型）、**opus**（编译器）、**perform**（MIDI 播放）、**kyma**（CLI 入口）。卡片之间用箭头连线，形成流水线。 | "整个工具链由四个模块组成：sonus 负责纯音乐理论建模，opus 负责编译，perform 负责 MIDI 回放，kyma 把它们串联成一条命令行管线。" |
| 0:20 – 0:25 | 镜头聚焦 sonus 模块。30 种音阶名称以环形排列旋转出现：Major、Dorian、Phrygian……画面切到日本调式 Hirajoshi、Insen 的特写。 | "内置 30 种音阶——从教会调式到日本平调子、从匈牙利吉普赛到阿拉伯音阶，开箱即用。" |
| 0:25 – 0:30 | 画面切换为和弦检测演示：左侧输入一组音高集合 `{D, F#, A, C}`，右侧弹出候选和弦列表，第一名高亮 **D7**。底部显示 Forte 音级集合分析：Prime Form、Interval Vector。 | "给它一组音高，它能自动识别所有可能的和弦解释，并按匹配度排序。同时内置 Forte 音级集合理论——正规形式、素形式、区间向量，学术级分析能力原生集成。" |
| 0:30 – 0:35 | 编译管线动画：左侧 `.tm` 文本文件 → 中间 Lexer/Parser 齿轮转动 → 右侧 `.bm` 二进制文件（十六进制字节流滚动）。底部标注：`BMIR v2 · compact binary IR`。 | "toki-musi 源文件经过词法分析、语法分析，编译为自定义二进制中间表示 BMIR——紧凑、高效、可直接加载演奏。" |
| 0:35 – 0:40 | 终端画面：`kyma compile canon.tm -o canon.bm` → `kyma perform canon.bm`。MIDI 波形可视化从终端底部涌出，形成音频频谱动画。 | "一条命令编译，一条命令演奏。MIDI 信号实时输出，从代码到声音，零中间步骤。" |

---

## 三、新语法（0:40 – 1:05）

| 时间 | 画面 | 旁白 |
|------|------|------|
| 0:40 – 0:46 | 全屏代码编辑器。逐行高亮展示 toki-musi 基础语法：音符 `C4`、`F#5`、`Bb4`，时值标记 `:4` `:2` `:8`，附点 `:4.`。每行出现时左侧对应钢琴键亮起。 | "toki-musi 的语法直觉而简洁：一个音名加八度就是一个音符，冒号后跟数字标记时值，附点用小数点表示。" |
| 0:46 – 0:52 | 代码区域切换为和弦记法：`[D maj]:2`、`[A 7]:2`、`[G maj7 / D]:2`。每个和弦出现时，画面右侧显示对应的五线谱和弦图。底部浮现 slash chord 标注。 | "和弦采用 lead-sheet 记法——方括号内写根音与性质，斜杠指定低音。支持七和弦、九和弦、挂留、添加音，一切你在真实乐谱中见到的符号。" |
| 0:52 – 0:58 | 画面分为上下两层。上层：track/section 结构代码 `track "melody" violin { section "A" repeat(2) { ... } }`。下层：对应的多轨时间轴可视化，小提琴轨和钢琴轨并行展开，小节线随 `|` 分隔符出现。 | "用 track 和 section 组织多轨结构，repeat 标记反复，竖线划分小节——像写总谱一样清晰地描述音乐的结构。" |
| 0:58 – 1:05 | 表情记法快闪蒙太奇：`@dyn(mf)` 力度标记 → `@cresc` 渐强波浪线 → `@pedal(sustain, on)` 踏板 → `3:2 { ... }` 三连音花括号 → `grace(C#5) D5` 装饰音滑入。每个标记出现时配合对应的音乐术语标注动画。 | "力度、渐强、延音踏板、三连音、装饰音、延音线——所有表情记号都是一等公民。不需要额外插件或特殊格式，语法本身就支持完整的音乐表达。" |

---

## 四、结尾（1:05 – 1:15）

| 时间 | 画面 | 旁白 |
|------|------|------|
| 1:05 – 1:09 | 画面回到开头的明亮渐变空间。一段完整的 Canon in D 代码从远处飞来，停稳在画面中央。代码自动编译，`.bm` 字节流从右侧涌出，化为 MIDI 音符粒子。 | "从文本到二进制，从二进制到声音——一条完整的管线，用 Rust 写成，为音乐而生。" |
| 1:09 – 1:13 | 粒子汇聚成 Kyma Logo。下方出现三行信息：`github.com/your-org/kyma`、`toki-musi v0.1.0`、`MIT License`。 | "Kyma——用代码的语言，写音乐的语言。" |
| 1:13 – 1:15 | Logo 微微脉动，如同呼吸。渐白淡出。 | （静默） |

---

## 制作备注

- **字体**：代码画面使用 JetBrains Mono 或 Fira Code，正文标题使用 Inter 或 Noto Sans SC
- **配色**：主背景 `#F8FAFC` → `#EEF2FF`（浅灰白到淡薰衣草渐变），强调色 `#6366F1`（靛蓝），辅助色 `#10B981`（翠绿），代码高亮 `#F59E0B`（琥珀黄），文字 `#1E293B`（深石板灰）
- **音效**：键盘敲击音（开头打字段）、编译齿轮音（管线段）、MIDI 钢琴音（结尾演奏段）
- **动画风格**：Remotion 实现，优先使用 spring 缓动，代码输入使用逐字符 reveal，图形元素使用 stagger 入场
